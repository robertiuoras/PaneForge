Done. Added `src/renderer/src/components/CardX.tsx` (one shared `<button className="card-x" aria-label="Dismiss">×</button>`) and rendered it as the first child in all seven listed cards (`AutoClearToast.tsx`, `MoveSoon.tsx`, `StopServer.tsx`, `ClientToast.tsx`, `UpdateToast.tsx`, `WhatsNewCard.tsx`, `Tips.tsx`), each wired to the safe dismiss named in the brief (Keep this session / Keep it open / Keep it running / local `gone` state for the rename toast / the existing Later, WhatsNew and Tips dismissals). CSS added a `.card-x` rule using only `var()` colours, hairline border on hover, no fill at rest, 44px on handheld, and changed the corner-stack override from `position: static` to `position: relative` (plus `padding-right` on each card) so the button anchors correctly without overlapping the first line. `scripts/activity-test.mjs` was updated to match the new `position: relative` rule and gained a new block asserting every card file imports and renders `<CardX`. Verified: `npm run typecheck` clean, `npm run test:activity` 110 checks passed, `npm run build` succeeded. Committed on `master` as `16b0a190` (`feat(corner): an X in the top-right of every corner card`), not pushed. Files outside this brief's scope (`src/shared/reclaim.ts`, `src/main/sessions.ts`, `src/renderer/src/App.tsx`, `scripts/reclaim-test.mjs`, `src/shared/types.ts`) were left untouched and unstaged, as they belong to another agent.
