# Brief: a restored pane's screen is torn until somebody presses Fix

Robert, 2026-09-02 (from the phone): "still major issue on frame being buggy and need to press fix to fix in terminal view".
Desk at the time: 7 panes restored at 14:37 local after the v0.8.188 update install; 5 came back ASLEEP (desk.json `asleep: true`), 2 live. The torn one is a terminal view he looked at after the restart.

Rules that already exist (read CLAUDE.md sections "A pane's two ends open at the same width", "A reopened pane comes back with what was on its screen" incl. "Presses Fix for itself", and docs/design-notes.md same titles) - the app is SUPPOSED to press Fix for itself (`repair()` once, `RESTORE_FIX_MS` 1.2s after output stops; a HIDDEN pane is FLAGGED not repaired; a mirror is refused). Something in that chain does not fire on the real desk.

Measure first, no editing on a theory:
1. `npm run build && npm run try -- --keep --remote-debugging-port=9333` with a desk that restores 3+ panes, at least one asleep, one hidden behind another tab. Then `npm run probe` reading, per pane, `window.__pf[id]` - term.cols vs pty cols, `replayCols`, whether `repair()` ran (add a counter on window if there is none), and a row-tear signature (a line that ends mid-word at col 79/119 and continues on the next row).
2. Cases to cover: asleep pane woken later (does the flag survive wake?), hidden pane shown later (does the flag ever get acted on?), a pane whose output never "stops" so the 1.2s quiet never comes.
3. State the numbers, then change ONE thing, re-measure, pin with `npm run test:restorefix` (needs a window) plus a headless test if the cause is in shared code.

Deliver: commit(s) on your lane, `npm run typecheck` + `npm test` green, then `node scripts/lane.mjs ready ...` as the lane hook tells you. No release - Robert asks for releases. Report the before/after numbers in the commit message.
