# Goal: choose a coherent assistant product and testable experience

Produce a decision-ready plan and comparable layout prototypes for the private PaneForge successor. This is a fresh planning session explicitly requested by Robert. Read `START-HERE.md` beside this file first and retrieve only the relevant existing research. Do not repeat the 100-project catalogue or load the old conversation.

Own `docs/research/successor-product-plan.md` and new layout prototypes under `docs/design/successor-layouts/` in your assigned PaneForge checkout. First save `docs/research/workflow-foundations/product-session-receipt.json` with actual cwd, pane ID, provider/native conversation ID if available and acknowledged status. A sibling session owns prompt/goal/memory/context workflow planning; do not change shared hooks.

Acceptance: (1) map Robert's main tasks across Taskdriver and PaneForge with one shared task/team concept; (2) compare Electron improvement versus Tauri plus existing React using migration costs, native/terminal compatibility, resource measurements and failure recovery, without promising rendering fixes; (3) distinguish personal assistant, persistent teammates, execution workers and occasional live voice; (4) propose a staged MVP with explicit deferrals and evidence gates before 100+ workers/public release; (5) compare a focused set of relevant competitors' navigation and code/chat/agent transitions from primary sources, distinguish observed UI, published testing and inference; (6) build three small comparable interactive layout alternatives using the same sample task, including left navigation and top/central mode control, preserving distinctive PaneForge/provider identity; (7) define a short user evaluation using task completion, wrong turns, recoverability and preference, not an invented conversion score.

Existing demo is in `docs/design/paneforge-directions/studio.html`; START-HERE records its limitations and rejected directions. React/Tauri choice and layout remain open. Verify current vendor claims from official sources only as needed. Use DOM/accessibility and headless checks first; screenshots at visual milestones. Existing authority to show design previews does not authorise closing unrelated tabs or controlling other work. Keep previews local, respect the desktop guard, preserve reduced motion and mobile access.

Planning and local prototypes only: no app migration, production release, external messages, new accounts, paid API use or recurring jobs. No worker swarm. Use the current included subscription/model, no paid fallback. Preserve other sessions' files and assigned lanes. Stop after a concrete reviewable plan, working prototypes with validation evidence, and a receipt naming unresolved decisions. Register this bounded goal only if native goal support is available; otherwise accurately call it a saved goal brief.
