# Goal: make the agent workflow understandable and measurable

Produce an executable improvement plan for Robert's prompt library, reusable goal library, second brain and Claude/Codex context/usage management. This is a fresh planning session, explicitly requested by Robert. Read `START-HERE.md` beside this file first, then only relevant linked evidence. Do not load the old transcript.

Own `docs/research/workflow-foundations/workflow-plan.md` in your assigned PaneForge checkout. Begin by saving `workflow-session-receipt.json` there with your actual cwd, pane ID, provider/native conversation ID if available, and acknowledged status. Do not guess identity. A sibling session owns product/layout planning.

Investigate the existing shared setup at `/Users/robertiuoras/Projects/claude-memory`: `claude-config/promptlib`, `promptlab`, `workflow-route.mjs`, provider hook entrypoints, shared goal briefs and retrieval consumers. Follow code-map and recall for unknown code/store discovery. Existing audit exclusions and report locations are in START-HERE. Do not re-audit unchanged completed items. Preserve native memory separation and private client boundaries.

Acceptance: (1) one canonical workflow map with source links, trigger, input, output and verification; (2) explain prompt versus goal and supply grounded reusable goal examples with outcome, scope, authority, acceptance, evidence, recovery/stop criteria; (3) distinguish configured/injected/retrieved/applied/verified memory use with a proposed bounded cross-provider test; (4) establish which usage metric Robert's reported 60% represents, or name unavailable evidence, and propose measurements for repeated context, output, cache, retries and worker overhead; (5) prioritise new defects by evidence/value and specify small repair tasks with tests; (6) give one fresh-agent-ready first implementation goal, with preconditions and file ownership.

Keep this session planning-only. Safe prompt/config repairs were previously requested but are being scoped here before execution. No install, shared skill edits, scheduler/auth changes, API spending, production operations or new worker sessions. Use your existing included subscription and selected model; no paid fallback. Do not run the prompt crafter merely to grade its own output. Its attribution/failure gaps are already documented. Do not claim token savings without measurement. Stop after saving the plan and a completion receipt with evidence and unresolved decisions. If native goal support exists you may register this bounded planning goal; otherwise label it a saved goal brief, not an active /goal.
