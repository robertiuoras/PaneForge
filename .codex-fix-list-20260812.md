[x] 1. why does it show codex model in card for just gpt-5.6-terra but not other cards for sol can you fix that? | verify: `PF_PORT=9334 node scripts/session-choice-test.mjs` showed `GPT-5.6 Sol` on a real pane card
[x] 2. can start pane and work automatically chosen by ai in remote paneforge on pc, I must first have access to paneforge on my mobile as well to see it running, and should we have filter to see each device different panes? Mobile like controlling remote can see both running and choose between them need constant link as well maybe not Cloudflare ? | verify: `npm run test:remote` 37/37 preserves remote agent/model/first task; `npm run test:phone` 121/121 and `npm run test:panesize` prove the phone client and safe desktop ownership
[x] 3. also allow easier copy in paneforge of anything that terminal outputs or your own prompt, its still way to difficult to copy and paste maybe if possible you can add custom button to copy? on big chunks of text? | verify: `PF_PORT=9334 node scripts/session-choice-test.mjs` drove the Copy button against a real pane and verified clipboard output, restoring the original clipboard afterward
